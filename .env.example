# The only value required for real Telegram authentication.
# Leave it empty to start the backend in setup mode.
# This backend does not need the Client Secret or bot token.
TELEGRAM_CLIENT_ID=

# Keep false for direct access. Set 1 when exactly one trusted reverse proxy is used.
# TRUST_PROXY=false
